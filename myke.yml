project: timetriggers

env_files:
  - .env

discover:
  - agent

tasks:
  apply_rules:
    cmd: |
      cat firestore.rules.gotmpl | gomplate > firestore.rules
      firebase deploy --only firestore:rules
      # firebase deploy --only firestore:indexes
